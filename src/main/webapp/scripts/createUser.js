App.controller('CreateUserCtrl', function($rootScope, UserService, $uibModal,
		$scope, $log, $uibModalStack) {

	var self = this;
	self.user = {
		firstName : null,
		lastName : '',
		address : '',
		email : '',
		ssoId: ''
	};

	var modalInstance;

	
});
